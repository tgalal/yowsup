language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
    - python setup.py install
# command to run tests
script: nosetests
branches:
  only:
    - master
    - develop
    - uniform_xml
